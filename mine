import 'parckage:flutter/material.dart';
void main() 
{ 
runApp(MyApp());

class MyApp extends StatelessWidget 
   {

          @override 
          Widget build (BuildContext context) 
       {
          return MaterialApp(
          
          title: 'FOOD MENU',
           debugShowCheckedModeBunner: false,
          theme: ThemeData ( 
                            primarySwatch: Colors.blue,
                           ),
          home: MyHomePage(),
          ):
       }
    }
}

class MyHomePage extends StatelessWidget 
{
  const MyHomePage ({Key key}): super (key: key);
  
  @override
  _MyHomePageState () => _MyHomePageState();
}

class _MyHomePage extends State <MyHomePage>
{
  @overrride
  Widget build (BuildContext context)
  {
    return Container (
           child: SafeArea(
           child: Scaffold(
                  body: Column (
                  children: [ 
                              
                              Row (
                               children: [ ImageIcon(AssetImage(xxxxxxxx), size: xx, color: Colors.black),
                              Row (
                               children: [ Icon (Icon.search),
                                           
                                           Icon (Icon.notification),
    )
  }
}
